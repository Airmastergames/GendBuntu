# Configuration Nixpacks pour Railway
# IMPORTANT: Configurez Root Directory = 'backend' dans Railway Settings

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm ci",
  "npx prisma generate"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm run start:prod"
